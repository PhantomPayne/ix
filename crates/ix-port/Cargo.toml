[package]
name = "ix-port"
version = "0.1.0"
edition = "2021"

[dependencies]
ix-core = { path = "../ix-core" }
netstat2 = "0.11.2"

[dev-dependencies]
ix-core = { path = "../ix-core", features = ["test_utils"] }
